<?xml version="1.0" encoding="UTF-8"?>
<!-- Declaración del encabezado XML, define la versión de XML (1.0) y la codificación de caracteres (UTF-8) -->

<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">
    <!-- Este es el elemento raíz que define la configuración de la aplicación web en un archivo web.xml de Java EE
         Se especifica el namespace XML de Java EE (xmlns), el namespace para el esquema XML (xmlns:xsi),
         y la ubicación del esquema XML de la versión 4.0 de web-app (xsi:schemaLocation).
         También se define la versión de la especificación web-app como 4.0 -->

    <display-name>PerfilUsuario</display-name>
    <!-- Define el nombre de la aplicación web que se mostrará en el servidor de aplicaciones -->
    <!-- En este caso, "PerfilUsuario" es el nombre que se mostrará en los logs y el servidor -->

    <welcome-file-list>
        <welcome-file>Perfil</welcome-file>
        <!-- Define la página de bienvenida para la aplicación.
             "Perfil" se considera como la página inicial que se mostrará al acceder a la aplicación.
             Esto significa que al ingresar a la raíz de la aplicación, se redirige automáticamente a la página "Perfil". -->
    </welcome-file-list>

</web-app>
